services:
  # Auth Service
  - type: web
    name: auth-service
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node server.js
    rootDir: auth-service
    envVars: []

  # Grocery Service
  - type: web
    name: grocery-service
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node server.js
    rootDir: grocery-service
    envVars:
      - key: MONGO_URI
        sync: false # User will provide the Atlas connection string manually in Render dashboard

  # Cart Service
  - type: web
    name: cart-service
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node server.js
    rootDir: cart-service
    envVars: []

  # Order Service
  - type: web
    name: order-service
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node server.js
    rootDir: order-service
    envVars: []

  # Frontend Service (Gateway)
  - type: web
    name: frontend-service
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node server.js
    rootDir: frontend-service
    envVars:
      - key: AUTH_SERVICE
        value: http://auth-service:10000
      - key: GROCERY_SERVICE
        value: http://grocery-service:10000
      - key: CART_SERVICE
        value: http://cart-service:10000
      - key: ORDER_SERVICE
        value: http://order-service:10000
